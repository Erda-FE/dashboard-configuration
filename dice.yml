version: 2.0
envs:
  TERMINUS_APP_NAME: DATAHUB-UI
  TERMINUS_TRACE_ENABLE: false
services:
  datahub-ui:
    ports:
    - 80
    envs:
      TERMINUS_TRACE_ENABLE: false
      TERMINUS_APP_NAME: datahub-ui
      DATAHUB_MASTER_HOST: manager-starter.test-4863.services.v1.runtimes.marathon.l4lb.thisdcos.directory
      DATAHUB_MASTER_PORT: 80
    hosts:
    - 127.0.0.1 datahub.terminus.io
    resources:
      cpu: 0.2
      mem: 256
      disk: 100
    deployments:
      replicas: 1
    expose:
    - 80
